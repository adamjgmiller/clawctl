# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`clawctl` is an agent-native control plane CLI for managing OpenClaw fleets. Licensed under GNU AGPL v3.

## Stack

Node.js/JavaScript project (inferred from `.gitignore`). No build system, dependencies, or source code have been added yet — this is a skeleton repository.

## Development Setup

No commands are defined yet. Once `package.json` exists, update this file with the actual build, test, and lint commands.
